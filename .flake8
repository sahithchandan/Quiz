[flake8]
max-complexity = 12
max-line-length = 130
exclude =
    # There's no value in checking cache directories
    __pycache__,
    # ignore migrations?
    **/migrations/,
    settings/,
    # No need to traverse our git directory
    .git,
